---
description: ".NET MySQL Persistence — EF Core + Pomelo MySQL persistence module for ASP.NET Core services"
summary: >
  Generates a Resources/Persistence.cs with AppDbContext and PersistenceExtensions
  using EF Core with the Pomelo MySQL provider (ServerVersion.AutoDetect).
  Consumed as a library by .NET service archetypes.
authors: ["Jimmie Fulton <jimmie@ybor.ai>"]
tags: ["library", "dotnet", "persistence", "mysql", "efcore", "pomelo"]

requires:
  archetect: "3.0.0"

templating:
  trim_blocks: true
  lstrip_blocks: true
